version: '3.8'

services:

    # Service - github-artifact-db-server
    github-artifact-db-server:
        container_name: github-artifact-db-server
        image: postgres:12.4
        restart: always
        ports:
            - "5432:5432"
        environment:
            - POSTGRES_USER=postgres
            - POSTGRES_PASSWORD=postgres
            - POSTGRES_DB=testdb

# Service - github-artifact-service
    github-artifact-service:
        container_name: github-artifact-service
        image: img/github-artifact-service:1.0.0
        restart: always
        ports:
            - "8099:8099"
        depends_on:
            - github-artifact-db-server

